package com.raffle.ticketing.system.domain.event;

/*
    DomainEvent is the marker interface to mark the class as a DomainEvent.And also to mark the entity class using the generic type,
 */
public interface DomainEvent <T> {
}
